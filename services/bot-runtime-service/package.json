{
  "name": "bot-runtime-service",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "Orchestrator: reads a subject's bot + config from Supabase, decrypts secrets, composes env, and runs the central-bot as one process per subject (PM2).",
  "main": "src/server.js",
  "engines": {
    "node": ">=18 <23"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "pg": "^8.12.0",
    "pm2": "^5.4.2"
  }
}
