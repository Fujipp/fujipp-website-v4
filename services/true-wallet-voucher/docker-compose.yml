services:
  true-wallet-voucher:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      HOST: 0.0.0.0
    ports:
      - "127.0.0.1:3611:3611"
    volumes:
      - ./data:/app/data
